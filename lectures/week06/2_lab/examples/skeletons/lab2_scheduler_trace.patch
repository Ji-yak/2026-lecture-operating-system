--- a/kernel/proc.c
+++ b/kernel/proc.c
@@ -440,6 +440,8 @@
     for(p = proc; p < &proc[NPROC]; p++) {
       acquire(&p->lock);
       if(p->state == RUNNABLE) {
+        // TODO: Add a printf here to trace context switches.
+        // Print cpuid(), p->pid, and p->name.
         // Switch to chosen process.  It is the process's job
         // to release its lock and then reacquire it
         // before jumping back to us.
